# Task Todo

- [x] 定位表单字段和 Prompt 生成逻辑，确认变更集中在 `index.html`
- [x] 新增“图面语言”单选框，提供“中文 / English”两种模式，默认中文
- [x] 将语言选择接入 Prompt，要求图中所有可见文字按所选语言输出，并明确约束分支标签、状态文案和代码标识符改写规则
- [x] 调整清空逻辑，重置语言选择到中文默认值
- [x] 通过 `js_repl` 模拟页面脚本，验证中英文 Prompt 片段和 `clearForm()` 重置行为

## Review

- `index.html` 现在会在生成的 Prompt 中写入“图面语言”以及一整段语言约束，明确要求图中标题、节点名、注释、连线标签、图例等统一使用所选语言。
- 中文模式额外强调使用“是/否”“通过/不通过”“成功/失败”等自然语言，不保留 `yes/no`、`true/false`、`if/else` 这类代码式表达。
- 英文模式要求整张图统一使用英文表达，同时保留 API、协议名、标准缩写等专业术语的原文形式。
- 验证方式：使用 `js_repl` 读取 `index.html` 的内联脚本，在 mock DOM 中分别生成中文和英文 Prompt，并确认 `clearForm()` 会恢复中文默认值且隐藏输出区域。
